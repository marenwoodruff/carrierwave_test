<% if @restaurant.new_record? %>
  alert("Failed to upload restaurant: <%= @restaurant.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#restaurants").append("<%= render(@restaurant) %>");
<% end %>